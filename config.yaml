# Project configuration for 07_CrSBr_Cryo4.
# Paths are relative to this file's location (Code/).
# The processing package reads these at runtime — it contains no paths of its own.

data_dir: ./TRR/raw_data
processed_data_dir: ./TRR/processed
reports_dir: ./TRR/reports
pl_dir: ./PL/raw_data
processed_pl_dir: ./PL/processed
pl_bfield_dir: ./PL/raw_data/2025-04-30/D8-bsweep
